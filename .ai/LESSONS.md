# LESSONS (Shorthack) — ошибки которые нельзя повторять

## L001 | Vite proxy ≠ реальный путь редиректа
❌ В `vite.config.js` прокси настроен на `/s`, а бэкенд отдаёт `short_url: "/{code}"`
✅ Либо проксировать `/{code}` нельзя (перехватит всё) — либо бэкенд должен отдавать `/s/{code}`
! Пока работает в dev, потому что редирект идёт напрямую на :8000. FIX: unified prefix `/s/`

## L002 | Сессии SQLAlchemy не закрываются
❌ В каждом эндпоинте `session = Session()` без `close()` → утечка соединений под нагрузкой
✅ `with Session() as session:` (контекстный менеджер) или Depends + yield
! SQLite терпит, PostgreSQL зато нет — чинить ДО миграции на Postgres

## L003 | random.choices ≠ криптостойкий
❌ Предсказуемые коды = перебор чужих ссылок
✅ `secrets.token_urlsafe(4)` для прод-кодов, random только для dev
! Не критично для MVP без приватных ссылок, критично для Pro-плана

## L004 | git
❌ `git add .`
✅ `git add` конкретные файлы
❌ секреты в коде
✅ только .env (в .gitignore)

## L005 | Keyword-деградация в RAG: ложные срабатывания коротких стемов
❌ `score > 0` при стемминге в 5 символов: слово «записаться» со стемом «запис» матчилось с «учётная запись» в любом регламенте с оценкой 1.0, из-за чего вопрос вне базы («как записаться в секцию по плаванию») находил регламенты и генерировал галлюцинацию вместо эскалации (FR-032).
✅ Для многословных запросов (≥2 токенов) требовать совпадение не менее 2 стемов (`min_score = 2`), а также обязательно использовать гейт на непустую выдачу по исходному вопросу до сплита подзапросов.

## L006 | Ручная валидация параметров JSON: null-значения в required полях
❌ Проверка `elif spec.get("required"):` только при отсутствии ключа (`if key in raw_params: val = raw_params[key]`). Если клиент передаёт `{"param": null}`, ключ есть, но значение `None` пропускает блок `if val is not None:` и не попадает в `resolved`, приводя к `KeyError` (HTTP 500) вместо HTTP 422.
✅ Проверять `if val is None and spec.get("required"): raise HTTPException(422)`. Если поле не required, но имеет `default` — подставлять `val = spec["default"]`.


